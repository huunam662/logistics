apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meu-logistic-core.fullname" . }}-test-connection"
  labels:
    {{- include "meu-logistic-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meu-logistic-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
