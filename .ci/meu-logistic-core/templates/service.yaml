apiVersion: v1
kind: Service
metadata:
  name: {{ include "meu-logistic-core.fullname" . }}
  labels:
    {{- include "meu-logistic-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meu-logistic-core.selectorLabels" . | nindent 4 }}
